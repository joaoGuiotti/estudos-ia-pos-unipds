{
    "name": "ecommerce-recomendations-with-ml",
    "version": "1.0.0",
    "description": "An e-commerce recommendation system with TensorFlow.js",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "start": "npm run build && browser-sync -w . --server --files \"index.html, data/*.json, dist/**/*.js, style.css\" --port 3000",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "ecommerce",
        "tensorflow",
        "recommendation",
        "javascript"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "browser-sync": "^3.0.4",
        "typescript": "^6.0.3"
    },
    "dependencies": {
        "@xenova/transformers": "^2.17.2",
        "chromadb": "^1.8.1"
    }
}
